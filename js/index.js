$(document).ready(function(){
	
    	$("#slider").bxSlider({
		  auto: true,
		  pager: true,
		  minSlides: 1,
		  maxSlides: 1,
		  slideWidth: 1000,
		  adaptiveHeight: true,
		  interval: 3000,
		  pagerType: 'short',
		  pagerSelector: '#pager',
		  randomStart: true
		});
	
});
